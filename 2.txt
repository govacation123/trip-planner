请在现有 result 页面的基础上进行增强，实现以下功能（注意：不要重写整个页面左侧的已有展示逻辑，仅做增量修改）：
1️页面布局重构 (Flex布局)
将页面整体改为 Flex 布局。
左侧：保持原有行程展示部分（自适应宽度 flex: 1）。
右侧：新增一个固定宽度（约 350px）的对话侧边栏（Chat Panel）。
2️侧边栏 UI 组件
顶部：聊天历史消息列表（可以滚动）。
底部：输入框（textarea）+ 提交按钮（发送图标或文字）。
状态：添加一个 isRefining（或类似命名）的 loading 状态。在请求期间，提交按钮需显示加载动画并禁用，防止重复点击。
3️API 对接与状态流转
当用户点击发送时：
将用户输入推入 history 数组（显示在聊天列表中，作为用户消息）。
清空输入框。
调用接口：POST /api/trip/refine （注意路径）。
请求体格式：
code
JSON
{
  "plan": 当前左侧正在展示的完整 plan 数据,
  "user_feedback": 用户输入的文字
}
处理响应：接口返回格式为 { "success": true, "data": {新计划对象} }。请提取 data 字段，并用它替换当前页面的 plan 响应式数据（这会触发左侧视图的自动更新）。
将后端返回成功（或失败）的提示作为系统消息推入 history 数组。
请结合当前项目使用的前端框架（Vue/React等），给我修改完整代码。
